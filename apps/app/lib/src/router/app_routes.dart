/// 検索ブランチのルートパス。
const searchPath = '/search';

/// 設定ブランチのルートパス。
const settingsPath = '/settings';

/// UI Style設定画面の、[settingsPath]からの相対パス。
const settingsUiStyleRelativePath = 'ui-style';

/// UI Style設定画面へ`pushNamed`する際に使う名前付きroute名。
const settingsUiStyleRouteName = 'settingsUiStyle';

/// Theme Mode設定画面の、[settingsPath]からの相対パス。
const settingsThemeModeRelativePath = 'theme-mode';

/// Theme Mode設定画面へ`pushNamed`する際に使う名前付きroute名。
const settingsThemeModeRouteName = 'settingsThemeMode';
